export const WEBSITE_HERO_PROMPT = `Create a stunning website hero section as a complete HTML document.

SPECIFICATIONS:
- Canvas: 1920x1080px
- Create a pixel-perfect above-the-fold hero section:
  - Navigation bar: logo left, nav links center, CTA button right
  - Hero: compelling headline (2-3 lines), subheading, two CTA buttons (primary + secondary)
  - Visual element: CSS-created abstract shape, gradient orb, or geometric composition
  - Background: dark or brand color gradient
  - Floating UI element (card, mockup, or device frame) suggesting the product
- Typography hierarchy: use Google Fonts matching brand
- Subtle animations suggested via CSS (gradient shift, floating element)
- Pixel-perfect spacing, professional grid alignment

Output ONLY the complete HTML document.`;

export const MOBILE_APP_SCREEN_PROMPT = `Create a mobile app icon screen mockup as a complete HTML document.

SPECIFICATIONS:
- Canvas: 1242x2688px (iPhone Pro Max)
- Create a realistic iPhone mockup with CSS:
  - Phone frame (rounded rectangle, notch/dynamic island, side buttons)
  - Screen with status bar (time, signal, battery)
  - Home screen or app launch screen showing brand app
  - App icon (logo in rounded square)
  - Wallpaper matching brand aesthetic
  - Screen content: branded app interface
- The app interface should show something meaningful:
  - Splash screen with logo animation suggestion
  - Or main app screen with brand UI
- Realistic phone shadow and reflection

Output ONLY the complete HTML document.`;

export const EMAIL_NEWSLETTER_PROMPT = `Create a professional email newsletter template as a complete HTML document.

SPECIFICATIONS:
- Canvas: 600x800px (standard email width)
- Table-based layout (email-safe HTML)
- Header: brand color background, logo, navigation links
- Hero section: image area placeholder + headline + CTA button
- Content sections: 2-column grid for feature blocks
- Footer: unsubscribe link, address, social icons
- Brand colors throughout
- Clean, scannable layout
- Large CTA buttons in brand primary color
- Mobile-responsive: single column on small screens

Output ONLY the complete HTML document.`;

export const PITCH_DECK_COVER_PROMPT = `Create a presentation cover slide as a complete HTML document.

SPECIFICATIONS:
- Canvas: 1920x1080px (16:9 widescreen)
- Premium presentation cover slide:
  - Full-bleed brand gradient or strong color background
  - Company logo (centered or top-left)
  - Presentation title: "Company Name — Investor Pitch 2024"
  - Subtitle: "Building the future of [industry]"
  - Bottom bar: presenter name, date, confidential notice
  - Decorative brand elements (shapes, patterns, lines)
  - Sophisticated typography using brand fonts
- Feel: Goldman Sachs meets Apple — serious money energy

Output ONLY the complete HTML document.`;
