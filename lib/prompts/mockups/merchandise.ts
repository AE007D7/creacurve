export const TSHIRT_MOCKUP_PROMPT = `Create a professional t-shirt mockup as a complete HTML document using CSS.

SPECIFICATIONS:
- Canvas: 1500x1500px
- Create a photorealistic flat-lay t-shirt using CSS shapes and gradients
- The shirt body: use CSS to create a realistic shirt silhouette with:
  - Main body (rectangle with slight trapezoid for fold effect)
  - Two sleeve shapes
  - Collar (V-neck or crew neck based on brand style)
  - Realistic wrinkle/fabric texture using CSS gradients and filters
  - Cast shadow beneath the shirt
- Shirt color: use brand primary color or a complementary neutral
- Logo placement: centered chest, appropriately sized
- Add fabric weave texture using CSS pattern or SVG filter
- Fold lines and subtle shadow depth
- Background: clean white or light gray with slight shadow

Technical CSS approach:
- Use absolute positioning for all components
- CSS clip-path for sleeve shapes
- Multiple box-shadows for depth
- SVG noise filter for fabric texture
- Subtle gradient overlay for 3D effect

Output ONLY the complete HTML document.`;

export const COFFEE_MUG_PROMPT = `Create a professional coffee mug mockup as a complete HTML document using CSS.

SPECIFICATIONS:
- Canvas: 1500x1500px
- Create a photorealistic ceramic mug using CSS:
  - Cylindrical body with perspective (slightly wider at top)
  - Handle on right side (CSS border-radius D-shape)
  - Inside opening showing coffee/darkness
  - Rim highlight at top
  - Realistic ceramic gloss using CSS gradient overlays
  - Bottom shadow/reflection
  - Steam effect (optional: animated CSS wavy lines)
- Mug color: white ceramic or brand primary color
- Logo/design wraps around the mug (centered on visible face)
- Background: warm studio-style gray gradient
- Light source from top-left

Output ONLY the complete HTML document.`;

export const TOTE_BAG_PROMPT = `Create a professional tote bag mockup as a complete HTML document using CSS.

SPECIFICATIONS:
- Canvas: 1500x1500px
- Create a photorealistic tote bag using CSS:
  - Main bag body with slight perspective/depth
  - Two handles (curved, thick straps)
  - Stitching detail lines
  - Fabric texture via CSS filter
  - Natural canvas/cotton color or brand color
  - Inner depth hint at top opening
  - Shadow beneath the bag
- Logo prominently centered on bag face
- Background: clean neutral gradient
- Premium feel — like from a design goods shop

Output ONLY the complete HTML document.`;

export const STICKER_SHEET_PROMPT = `Create a sticker sheet mockup as a complete HTML document.

SPECIFICATIONS:
- Canvas: 1500x1500px
- Show 6-8 sticker designs arranged on a white backing sheet with die-cut borders
- Sticker types to include:
  1. Primary logo sticker (circle or custom shape)
  2. Brand name text sticker
  3. Tagline sticker
  4. Icon-only sticker (small, fun)
  5. Geometric brand shape sticker
  6. "Made by [Brand]" stamp-style sticker
- Each sticker has a subtle white border and die-cut shadow
- Backing sheet has slight cream tint with horizontal peel lines
- Some stickers slightly overlapping or at angles
- Vibrant colors, brand palette throughout

Output ONLY the complete HTML document.`;
